import 'del.dart';

void main() {
  // Del del = Del('lay');
  // print(del.name);
  // Del del1 = Del('aefs');
  // print(del.name);
  // print(del1.name);
  List<int> a;
  a = [1, 2, 3, 4, 5, 6];

  // a = a.map((e) => e * 2).toList();
  a.forEach((element) => print(a));
}
